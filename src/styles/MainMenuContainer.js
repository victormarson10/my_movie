import { styled } from "styled-components";

export const MainMenuContainer = styled.nav`
    ul {
        List-style: none;
        padding: 0;

        li {
            display: inline-block;
            margin-right: 10px;
        }
    }
`